# VITE_CODESPACE_NAME must be defined when running the frontend in GitHub Codespaces.
# The app uses it to build API URLs like:
# https://${VITE_CODESPACE_NAME}-8000.app.github.dev/api/users/
VITE_CODESPACE_NAME=your-codespace-name
